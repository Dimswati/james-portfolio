import React from 'react'

type Props = {}

const NotFound = (props: Props) => {
  return (
    <main>
        <section className='container'>Not welder does not exist</section>
    </main>
  )
}

export default NotFound